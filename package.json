{
  "name": "lab-devops",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run",
    "test:smoke": "node scripts/smoke-verify.mjs",
    "build:artifact": "npm run build && node scripts/create-build-artifact.mjs"
  },
  "dependencies": {
    "nuxt": "^4.4.5",
    "vue": "^3.5.34",
    "vue-router": "^5.0.6"
  },
  "devDependencies": {
    "vitest": "^4.1.6"
  }
}
